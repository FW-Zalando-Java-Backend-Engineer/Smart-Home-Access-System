public class UserAccessLevel {

    public static String getAccessLevel(String role) {
        // TODO: Use if-else or nested if-else to return access level
        // "Parent" → "Full Access"
        // "Child" → "Limited Access"
        // "Guest" → "Guest Access"
        // otherwise → "No Access"
        return "Access level not determined";
    }
}
